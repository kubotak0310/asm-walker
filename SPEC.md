# AsmWalker — プロジェクト仕様書

> 最終更新: EN/JA 言語切り替え実装（2026-05-09）

---

## プロジェクト概要

C言語学習者・組み込みエンジニア向けに、アセンブラ命令のステップ実行とスタック・レジスタの動きをブラウザ上で可視化する学習ツール。

**開発動機**: アセンブラ学習時にスタックとレジスタの動きを直感的に理解できるツールが存在しないという課題から出発。特にARM Cortex-M（組み込み業務で最も使われるアーキテクチャ）に特化した可視化ツールは世界的にも存在しない。

---

## ターゲットユーザー

- C言語を学び始めた学生・エンジニア（アセンブラを初めて学ぶ人）
- ARM Cortex-M系（STM32, nRF52 等）を扱う組み込みエンジニア
- x86とARMの違いを比較学習したい人

---

## 機能要件

### フェーズ1（MVP） ✅ 完了（Claude.ai プロトタイプ済み）

#### コアシミュレーター
- [x] x86-64 アセンブラのステップ実行（前後ステップ移動可）
- [x] ARM Cortex-M (Thumb-2 / ARMv7-M) のステップ実行
- [x] x86 / ARM のアーキテクチャ切り替えスイッチ
- [x] x86とARMのシミュレーターを完全分離（混在によるバグを防ぐ設計）
- [x] 各ステップのイミュータブルなスナップショット配列（前後移動をO(1)で実現）

#### 表示パネル
- [x] スタックメモリの可視化（アドレス・値・ラベルバッジ）
- [x] 汎用レジスタの表示と変化ハイライト（緑）
- [x] SP / LR / PC などの特殊レジスタ表示（変化で橙）
- [x] Cソースコードとアセンブラの対応行ハイライト（ステップ同期）
- [x] 命令の日本語説明パネル（命令・効果・対応Cコードを表示）
- [x] x86 vs ARM 差分パネル（x86/ARM両モードで表示、画面最下部、デフォルト非表示・トグルボタンで開閉）
- [x] 実行モード表示（Thread Mode / Handler Mode）
- [x] スタックフレームの色分け表示（関数ごとに色を変える）

#### スタックメモリのセルハイライト
- [x] ポインタ値（アドレス）が入っているセル → 紫色ハイライト
- [x] 配列要素のセル → 緑色ハイライト
- [x] HW自動処理（Cortex-M割り込み退避）のセル → 橙色ハイライト
- [x] 新たに積まれたセルの強調表示

#### Cサンプルコード（ARM、5種）
- [x] 関数呼び出し（`add(int,int)` → `main`）
- [x] 条件分岐（if/else、比較）
- [x] ポインタ渡し（`double_it(int*)` で参照渡し）
- [x] 配列を関数に渡す（先頭アドレス渡し）
- [x] スタックフレーム（3層ネスト）

#### スタックフレームビジュアライザ（統合済み）
- [x] main → add(3,4) → multiply(s,2) の3層ネストをリアルタイム図示
- [x] フレームごとに色分け（main=紫、add=緑、multiply=橙）
- [x] x86 / ARM の切り替えで両方のフレーム構造を比較
- [x] 全プリセット統合版への組み込み完了（FrameViz コンポーネントとして実装）
- [x] スタックメモリの左ボーダーでもフレーム帰属を色表示（FrameViz と色統一）

---

### フェーズ2（ARMパーサー + 自由入力） ✅ 完了 → 自由入力UIはモード統合で削除

**実装方針: ARM先行、x86後追い**

- [x] 2パス ARM パーサー（`src/core/arm/parser.ts`）
  - [x] 大文字小文字の正規化
  - [x] `;` `@` コメントの除去
  - [x] `[SP, #offset]` 形式のオペランド解析
  - [x] ラベル定義の解析（`LABEL:` / `.L0:` / `square(int):` 形式）
  - [x] ジャンプ命令のラベル前方参照解決（2パス）
  - [x] Compiler Explorer 出力形式（`@` コメント・関数シグネチャ付きラベル）対応
- [x] ARM インタープリタ（`src/core/arm/interpreter.ts`）
  - [x] データ転送: MOV/MVN
  - [x] 算術: ADD/ADC/SUB/SBC/RSB/MUL/MLA/MLS/SDIV/UDIV
  - [x] 論理・シフト: AND/ORR/EOR/BIC/LSL/LSR/ASR/ROR
  - [x] 比較: CMP/CMN/TST/TEQ
  - [x] メモリ: LDR/STR/LDRB/STRB/LDRH/STRH
  - [x] スタック: PUSH/POP（PC込みのリターン対応）
  - [x] 分岐: B+条件コード全種・BL・BX・CBZ/CBNZ・NOP
  - [x] Sサフィックス（フラグ更新）・条件コード評価
  - [x] スタックフレーム自動追跡（BL/BX/PUSH/POP 連動）
- [x] 動的トレーサー（`src/core/arm/tracer.ts`）— `main:` ラベルをエントリポイントとして実行
- [x] 入力エラーの検出とエラーメッセージ表示
- ~~ARMアセンブラ自由入力UI（`FreeInputPanel.vue`）~~ → モード統合時に削除

**UI 改善（フェーズ2で併せて実施）**
- [x] ← / → キーボードショートカット（ステップ移動）
- [x] スタックパネル：フレーム内の未初期化領域を `. xxxxxxxx` で表示
- [x] 説明パネル：フル命令名（STORE REGISTER 等）・ARM仕様書準拠構文・構文記法ガイド（`?`ボタン）
- [x] effect 文字列の16進数表示統一

---

### フェーズ3（Cコンパイル連携） ✅ 完了

- [x] ブラウザ上でCソースコードを入力（CodeMirror 6 Cエディタ、`CCompilePanel.vue`）
- [x] Godbolt Compiler Explorer API 経由でコンパイル
  - ブラウザから Godbolt に直接 POST（CORS 許可済み・開発時・本番時ともに同一）
- [x] コンパイル結果（アセンブラ）とCソースの対応行を自動マッピング（`source.line` → `cLineMap`）
- [x] 対応コンパイラ: `carm1121`（ARM GCC 11.2.1）・`armug1320`（13.2.0）・`armug1430`（14.3.0）・`x86-64g1420`（x86-64 GCC 14.2.0）
- [x] コンパイラオプション選択（最適化レベル `-O0` / `-O1` / `-O2`・追加フラグ入力）
- [x] デフォルトフラグ: `-mcpu=cortex-m3 -mthumb`（SP相対アクセス・Cortex-M学習に最適）
- [x] ARMパーサー強化（実コンパイラ出力対応）:
  - fp/sl/ip レジスタエイリアス（r11/r10/r12）
  - シフト付きオペランド（`r1, lsl #2`）
  - pre/post-indexed アドレッシング（`[r0]!`・`[r0], #4`）
- [x] x86コンパイルモード: Intel構文（`-masm=intel`）で parseX86 + traceX86 によるフルステップ実行
- [x] 到達不能関数のグレーアウト表示（最適化でインライン展開された関数への注釈）
- [x] サンプルCコード（`src/samples/index.ts` — 6種: スタックフレーム・関数呼び出し・条件分岐・ループ・ポインタ渡し・配列）

**ARM ABI 可視化（フェーズ3追加）**
- [x] RegisterPanel: `bl` 命令時に引数レジスタ（r0〜r3）に「引数N」バッジ（青）表示
  - Cソース解析で引数個数を特定し、実際に使う分だけバッジを表示
- [x] RegisterPanel: `bx lr` / `pop {pc}` 時に r0 に「戻り値」バッジ（黄）表示
- [x] CCompilePanel バー: コンパイル後の実行状態を4段階で表示
  - コンパイル成功（初期）/ 「→ funcName(r0=val, r1=val) 呼び出し」/ 「▶ funcName() 実行中」/ 「✅ funcName() 実行完了」

**x86-64 ABI 可視化（フェーズ3後期）**
- [x] RegisterPanel: `call` 命令時に引数レジスタ（rdi/rsi/rdx/rcx/r8/r9）に「引数N」バッジ表示
- [x] RegisterPanel: `ret` 命令時に rax に「戻り値」バッジ表示

**RegisterPanel バッジ仕様（ARM/x86 共通）**
- [x] 「引数N」バッジ（青）: 関数呼び出し命令（`bl`/`call`）直前ステップで、引数として使われるレジスタに表示
- [x] 「戻り値」バッジ（黄）: 関数復帰命令（`bx lr`/`pop {pc}`/`ret`）直前ステップで、戻り値レジスタ（r0/rax）に表示
- [x] 「ptr」バッジ（紫）: メモリ参照命令（`ldr`/`str`/`mov [mem]`）実行ステップで、`[reg]` のベースレジスタに表示
  - フレームポインタ・スタックポインタを base に使う場合は除外（ARM: sp/fp/r7/r11、x86: rsp/rbp）
  - ローカル変数アクセスと実際のポインタ参照を区別するための除外ルール
- [x] CCompilePanel: x86サンプル5種を optgroup で追加（`X86_SAMPLES`）
- [x] SpecialRegPanel: x86 時に RSP/RBP/RIP のラベルを表示
- [x] ExplainPanel: arch に応じて x86/ARM の mnemonics を切り替え

**モード統合（フェーズ3後期）**
- [x] プリセットモード・自由入力モードを廃止し、Cコンパイルモード一本化
  - 手書きプリセット（`src/presets/`）→ `src/samples/index.ts` の軽量Cコードテンプレートに置き換え
  - `PresetSelector.vue`・`GuidePanel.vue`・`FreeInputPanel.vue` 削除
  - アセンブラコメント区切り `@` → `;`、コメント色を明るく変更

**UI 改善（フェーズ3で併せて実施）**
- [x] CCompilePanel: コンパイル後はエディタを薄いバーに切り替え（再編集ボタンで戻る）
- [x] CSourcePanel: コンパイルモードではコンパイル済み時のみ表示（冗長な二重表示を解消）

**学習ガイド・ナビゲーション（2026-05追加）**
- [x] `public/about.html`: ツール概要・技術構成・アドレス表示の仕組みを説明するページ
- [x] `public/guide/asm-reading.html`: アセンブラの読み方ガイド（命令フォーマット・ARM/x86比較）
- [x] `public/guide/stack.html`: スタックの仕組みガイド（SP・フレーム・PUSH/POP）
- [x] `public/guide/function-call.html`: 関数呼び出しの仕組みガイド（ABI・引数・戻り値・スタックフレーム）
- [x] `public/guide/branch.html`: 条件分岐ガイド（フラグレジスタ・条件コード・if/else/ループ）
- [x] `public/guide/pointer.html`: ポインタとアドレスガイド（アドレス・間接参照・配列との関係）
- [x] `public/guide/registers.html`: 汎用レジスタ一覧（ARM/x86-64/RISC-V 3アーキ対応、ABI規定・呼び出し規約を初学者向けに解説）
- [x] `public/guide/flags.html`: フラグレジスタ解説（ARM CPSR / x86 RFLAGS / RISC-V の「フラグなし」設計）
- [x] ヘッダーに「ガイド▼」ドロップダウンナビゲーションを追加（全ガイド + About を新しいタブで開く）
  - CSS `group-hover` + `pt-1` パディングでホバー時の隙間問題を解消
- [x] `RegisterPanel.vue` ヘッダーに `registers.html` へのアイコンリンクを追加
- [x] `SpecialRegPanel.vue` フラグラベルに `flags.html` へのアイコンリンクを追加（右端揃え）

---

### フェーズ4（機能拡張） 🔄 一部完了

**英語対応（UI）✅ 完了**
- [x] vue-i18n v11 による EN/JA 切り替え（`src/i18n/` ディレクトリ）
- [x] ブラウザ言語の自動検出（`navigator.language` で初期ロケール決定）
- [x] localStorage 永続化（`asm-walker-locale` キー）
- [x] 全 Vue コンポーネントの UI 文字列を `$t('key')` に置換（12ファイル）
- [x] ARM インタープリタの `explain` / `comment` をロケール対応（`ARM_EXPLAINS` / `ARM_COMMENTS` 辞書）
- [x] サンプル名を `{ ja: string; en: string }` 形式に変更
- [x] ヘッダーに EN/JA セグメントコントロール（AppHeader.vue に分離）
- [ ] ガイドページ（`public/guide/*.html`）の英語化（別タスク・量が多いため後回し）

**RISC-V 対応（feature/rv32 ブランチ）✅ 完了**
- [x] `src/core/rv32/` — parser / interpreter / tracer / mnemonics の4ファイル
- [x] RV32IM（基本整数命令 + M拡張）対応
- [x] ABI名レジスタ表示（zero/ra/sp/a0-a7/s0-s11/t0-t6、隣に `(xN)` 表記）
- [x] `SpecialRegPanel.vue`: RISC-V 時はフラグセクションを非表示（フラグレジスタが存在しないため）
- [x] サンプル: `RV32_SAMPLES`（関数呼び出し・条件分岐・ループ・ポインタ渡し・配列）

**RISC-V Compressed拡張対応（Step 2）✅ 完了**
- [x] コンパイラフラグを `-march=rv32im` → `-march=rv32gc` に変更（C拡張を含むコンパイラ出力を受け入れる）
- [x] パーサーで `c.addi` / `c.lw` 等 `c.` プレフィックス命令をスキップ（防御的実装）
  - **重要な設計知識**: Godbolt が表示するのは GCC が生成した `.s` テキストであり、`c.` 命令への変換はその後段のアセンブラ (gas) がバイナリ生成時に行う。そのため Godbolt テキスト出力に `c.` は絶対に現れない。パーサーの `c.` スキップロジックは永遠にトリガーされないが、安全策として残している。
  - `-march=rv32gc` への変更の実効は「アセンブラがバイナリをより小さくエンコードできる」点にあり、Godbolt テキスト出力の内容自体は変わらない。

**未着手**
- [ ] ユーザー認証 + コード保存・共有機能（Supabase Auth + PostgreSQL）
- [ ] 公開URLの短縮リンク（作ったコードをURLで共有）

---

## 非機能要件

- ブラウザのみで動作（インストール不要）
- モバイル対応（レスポンシブ）
- 日本語/英語切り替え対応（EN/JA トグル、localStorage 永続化、ブラウザ言語自動検出）
- ページ読み込みから3秒以内に操作可能な状態になること
- モダンブラウザ対応（Chrome / Firefox / Safari / Edge 最新版）

---

## 画面構成

### メインビジュアライザ

```
┌──────────────────────────────────────────────────────┐
│ [x86-64] [ARM Cortex-M]                               │
│ [◀ 戻る] [次のステップ ▶] [リセット]  ステップ N/M    │
├──────────────────────────────────────────────────────┤
│ Cコンパイルパネル（CCompilePanel）                      │
│  ・コンパイル前: CodeMirror Cエディタ + サンプル選択    │
│  ・コンパイル後: 薄いバー（コンパイラ/状態表示）         │
│    状態: 成功 / 呼び出し中 / 実行中 / 実行完了          │
├─────────────────────┬────────────────────────────────┤
│ C ソースコード        │ アセンブラ                      │
│（コンパイル後のみ表示）│ ・配列要素バッジ                  │
│ ・現在行ハイライト    │ ・コメントは `;` で表示           │
│   (緑=SW, 橙=HW)    │                                  │
├─────────────────────┴────────────────────────────────┤
│ 命令の説明パネル                                        │
│  ・位相バッジ / 命令 / 説明 / 実行結果 / 対応Cコード     │
│  ・配列要素ノート（配列命令の場合のみ）                   │
├────────────┬──────────────┬───────────────────────────┤
│ 汎用レジスタ │ SP/LR/PC    │ スタックメモリ               │
│ 変化→緑    │ SP変化→橙   │ アドレス・値・ラベルバッジ    │
│ 引数バッジ青 │ Mode表示     │ 緑=配列要素                 │
│ 戻り値バッジ黄│             │ SP行→橙背景                 │
│ ptrバッジ紫 │              │                             │
└────────────┴──────────────┴───────────────────────────┘
│ x86 vs ARM 差分パネル（最下部、デフォルト非表示）         │
└──────────────────────────────────────────────────────┘
```

### スタックフレームビジュアライザ（フェーズ1.5で統合）

```
┌──────────────────────────────────┐
│ [x86-64] [ARM]  ステップ実行      │
├─────────────────┬────────────────┤
│ Cソース         │ フレーム図      │
│ ASM             │ ┌──────────┐   │
│                 │ │multiply()│ 橙 │ ← 最深部
│                 │ ├──────────┤   │
│                 │ │  add()   │ 緑 │
│                 │ ├──────────┤   │
│                 │ │  main()  │ 紫 │ ← スタック底
│                 │ └──────────┘   │
│                 │ 使用量バー      │
├─────────────────┴────────────────┤
│ 命令の説明                        │
└──────────────────────────────────┘
```

---

## サンプル詳細仕様（`src/samples/index.ts`）

手書きプリセットを廃止し、Cコードテンプレート＋コンパイラ設定のみを保持。コンパイルはGodbolt API経由で行う。

### ARM サンプル（6種）

| ID | 名前 | Cコードの概要 | 主な学習ポイント |
|---|---|---|---|
| `stackFrame` | スタックフレーム | `main → funcA → funcB` 3層ネスト | フレーム生成・解放、FrameViz で色分け確認 |
| `funcCall` | 関数呼び出し | `main → add(3,5)` | BL命令、引数渡し(R0/R1)、戻り値(R0)、LRによる戻りアドレス保存 |
| `branch` | 条件分岐 | `compare(a,b)` で if/else | CMP+フラグ更新、BLT/BEQ条件分岐 |
| `loop` | ループ | `sum_to(5)` で 1〜5 の総和 | CMP+BLE でループバック、カウンタのインクリメント |
| `pointerPass` | ポインタ渡し | `double_it(int*)` で `*p = *p * 2` | &varはアドレス取得、LDR/STRの ptr バッジで参照を可視化 |
| `array` | 配列を関数に渡す | `sum_array(a, 3)` | 先頭アドレスのみ渡される（コピーなし）、`[r4,#n]` オフセットアクセス |

**共通コンパイラ設定**: `carm1121`（arm-none-eabi gcc 11.2.1）・`-O0`・`-mcpu=cortex-m3 -mthumb`

---

## 対応命令セット（フェーズ1プロトタイプ時点）

### x86-64
`push rbp`, `pop rbp`, `mov rbp,rsp`, `mov rsp,rbp`, `sub rsp,n`, `add rsp,n`, `mov reg,n`, `mov reg,reg`, `mov [rbp-n],reg`, `mov reg,[rbp-n]`, `mov [rax],n`, `mov reg,[rax]`, `lea reg,[rbp-n]`, `add reg,reg`, `add reg,[rbp-n]`, `sub reg,reg`, `imul reg,reg`, `xor reg,reg`, `idiv reg`, `call label`, `ret`, `cmp reg,reg`, `test reg,reg`, `jl label`, `jz label`, `mov [rdx+n]`, `mov [rdi]`

### ARM Cortex-M (Thumb-2 / ARMv7-M)
`MOV Rn,#n`, `MOV Rn,Rm`, `ADDS Rd,Rs,Rt`, `SUBS Rd,Rs,Rt`, `MUL Rd,Rs,Rt`, `SDIV Rd,Rs,Rt`, `CMP Rn,Rm`, `BLT label`, `CBZ Rn,label`, `BL label`, `BX LR`, `PUSH {regs}`, `POP {regs}`, `LDR Rn,[Rm]`, `LDR Rn,[Rm,#n]`, `LDR Rn,[SP,#n]`, `STR Rn,[Rm]`, `STR Rn,[SP,#n]`, `SUB SP,SP,#n`, `ADD SP,SP,#n`, `ADD Rn,SP,#n`

---

## Cortex-M 割り込み仕様（実装済み）

- ハードウェア自動退避レジスタ（例外スタックフレーム）: `R0, R1, R2, R3, R12, LR, PC, xPSR`（8レジスタ = 32バイト）
- 積み順（高アドレスから低アドレスへ）: `xPSR → PC → LR → R12 → R3 → R2 → R1 → R0`
- EXC_RETURN 値:
  - `0xFFFFFFF9`: Thread Mode / MSP使用（ベアメタル最一般的） ← **実装済み**
  - `0xFFFFFFFD`: Thread Mode / PSP使用（RTOS）
  - `0xFFFFFFF1`: Handler Mode / MSP使用（ネスト割り込み）
- HWステップ: `type: 'hw'` フラグで区別し、UI側で橙色ハイライトと「ハードウェアが自動実行 — アセンブラ命令は存在しません」バナーを表示

---

## アーキテクチャターゲット

| コア | アーキテクチャ | 代表マイコン | 対応状況 |
|---|---|---|---|
| Cortex-M3 | ARMv7-M | STM32F1/F3 | ✅ 対応 |
| Cortex-M4 | ARMv7E-M | STM32F4, nRF52 | ✅ 対応（SDIVあり） |
| Cortex-M7 | ARMv7E-M | STM32H7 | ✅ 対応 |
| Cortex-M0/M0+ | ARMv6-M | STM32F0 | ⚠️ SDIVなし（将来的に分岐） |
| Cortex-M33 | ARMv8-M | STM32L5, nRF9160 | 🔲 フェーズ4（TrustZone） |

---

## スタックアドレス設定

| アーキテクチャ | ベースアドレス | 備考 |
|---|---|---|
| x86-64 | `0x7FFF00` | 一般的なユーザースタック領域を模倣 |
| ARM Cortex-M | `0x20008000` | STM32等のSRAM末尾を模倣 |
| RISC-V RV32 | `0x00080000` | スタック上端（テキスト先頭は `0x00010000`） |

---

## バックエンド方針

フェーズ1・2はフロントエンドのみで完結（バックエンド不要）。

| フェーズ | バックエンド | 実装方針 |
|---|---|---|
| 1・2 | 不要 | フロントのみ |
| 3 | 不要（フロントのみ） | Godbolt API が CORS 許可済みのため直接呼び出し |
| 4（将来） | Supabase | Auth + PostgreSQL でコード保存・共有 |

---

## 開発ロードマップ

```
フェーズ1 ✅ 完了（Claude.aiプロトタイプ）
  └─ 6プリセット（x86/ARM切り替え）+ 割り込みプリセット（ARM専用）
  └─ 学習ガイドパネル（各プリセットに個別のガイド）
  └─ ポインタ・配列の可視化（紫/緑ハイライト）
  └─ スタックフレームビジュアライザ（3層ネスト・別ウィジェット）

フェーズ1.5 ✅ 完了
  └─ Vue 3 + TypeScript + Vite でプロジェクトセットアップ
  └─ スタックフレームビジュアライザを全プリセット統合版に組み込む
  └─ ARM側デバッグ完了（ステップモデル修正・スタック表示バグ修正等）
  └─ Vercel デプロイ・公開（未実施）

フェーズ2
  └─ 自由入力テキストエリア + 命令パーサー（基本命令セット）
  └─ ここで一度公開してフィードバックを収集

フェーズ3
  └─ Godbolt API連携（フェーズ2のパーサー強化が前提）

フェーズ4
  └─ RISC-V、ユーザー保存・共有、TrustZone
```

---

## 既存ツールとの差別化

| ツール | 差分 |
|---|---|
| AsmWalker (asm.diveintosystems.org) | ARM非対応・C対応表示なし |
| Compiler Explorer (godbolt.org) | ステップ実行なし・スタック可視化なし |
| VisUAL | デスクトップアプリ・C対応なし・Cortex-M特化なし |

**このツールの独自価値**: Cortex-M Thumb-2特化 + ステップ実行 + Cソース対応ハイライト + 日本語説明 + 学習ガイド + ポインタ/配列の可視化 + スタックフレーム色分け図 = 世界的に類似ツールなし
